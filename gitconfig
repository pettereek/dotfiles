[user]
	name = Petter Machado
	email = petter@machado.se
[alias]
	st = status
	br = branch
	co = checkout
	diffs = diff --cached
	logg = log --graph --format=format:'%C(dim white)%h%C(reset) %s %C(cyan)%an %C(reset)%C(dim white)%ar%d%C(reset)%+b'
[push]
	default = simple
[core]
	editor = vim
[pull]
	rebase = true
[url "git@github.com:"]
	insteadOf = https://github.com/
